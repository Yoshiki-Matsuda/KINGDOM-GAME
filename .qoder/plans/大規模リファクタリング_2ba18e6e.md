# 大規模リファクタリング計画

## Task 1: バグ・デッドコード修正（クイックウィン）

最初に取り掛かり、リスクの低い修正で基盤を整備する。

**1a. DEV_MODE ハードコード修正**
- `src/ui/home-screen.ts` L63-64: `const DEV_MODE = true` を `import.meta.env.DEV` に変更

**1b. デッドコード削除**
- `src/home-screen-build.ts` L50: 到達不可能な `!selectedTile` チェックを削除
- `src/ui/home-screen.ts` L48-51: 未使用変数 `_buildMenuTarget`, `_buildMenuDocListener` と `void` 抑制を削除

**1c. flea-market-screen.ts のイベントリスナーリーク修正**
- `src/ui/flea-market-screen.ts`: `renderSellTab()` で永続DOM要素に `addEventListener` が重複蓄積する問題を修正。イベント委譲パターンに変更

---

## Task 2: フロントエンド — store.ts の Valtio 移行

**2a. Valtio 導入**
- `npm install valtio` でインストール
- `src/store.ts`（193行、20+の `export let` + setter）を Valtio の `proxy()` / `useSnapshot()` ベースに書き換え
- `src/store-actions.ts` を Valtio の `subscribe` / `snapshot` ベースに更新

**2b. 全コンシューマーの更新**
- `export let` + setter を直接 import している全ファイル（`app-render.ts`, `home-screen.ts`, `formation-screen.ts`, `log-panel.ts` 等）を `proxy` オブジェクトからの読み取りに変更
- `setGameState(s)` -> `Object.assign(store.gameState, s)` 等

---

## Task 3: フロントエンド — 巨大ファイル分割

**3a. formation-screen.ts (962行) を分割**
- `src/ui/formation-hub-view.ts` — hub/unitsビュー切り替え、ユニット一覧描画
- `src/ui/formation-monster-view.ts` — モンスタービュー、キャラピッカー
- `src/ui/formation-stat-dialog.ts` — ステータス配分ダイアログ
- `src/ui/formation-screen.ts` — 画面全体のエントリポイント（200行程度）

**3b. log-panel.ts (930行) を分割**
- `src/ui/log-parser.ts` — `parseLogsToHistory()` の365行パーサー関数をクラス化（`LogParser` クラス）
- `src/ui/log-battle-view.ts` — 戦闘ログ表示コンポーネント
- `src/ui/log-panel.ts` — パネルUI本体（300行程度）

**3c. map-view.ts (928行) を分割**
- `src/game/map-renderer.ts` — PixiJS描画ロジック
- `src/game/map-interaction.ts` — クリック・ドラッグ等のインタラクション
- `src/game/map-view.ts` — エントリポイント（300行程度）

---

## Task 4: フロントエンド — コード重複排除

**4a. app-render.ts のマップ描画重複を統合**
- `render()` と `renderMapSession()` の重複ロジックを `applyMapVisibility()` 関数に抽出

**4b. characters.ts の4重定義を統合**
- `CHARACTER_STATS`, `CHARACTER_NAMES`, `CHARACTER_RARITY`, `CHARACTER_RACES` を1つの `CHARACTERS: Record<number, CharacterDef>` に統合
- 既存のアクセサ関数（`getCharName()`, `getCharStats()` 等）を更新

**4c. game-state.ts の attackAction パラメータ過多修正**
- 10個のパラメータを options オブジェクトパターンに変更

---

## Task 5: バックエンド(Rust) — 巨大ファイル分割

**5a. combat.rs (1552行) を分割**
- `server/src/model_actions/combat_turn.rs` — ターン管理、行動順序決定
- `server/src/model_actions/combat_damage.rs` — ダメージ計算、吸収、撃破
- `server/src/model_actions/combat_rewards.rs` — 報酬計算、ログ生成
- `server/src/model_actions/combat.rs` — エントリポイント（400行程度）

**5b. ai_actions.rs (1378行) を分割**
- `server/src/ai_exploration.rs` — 探索派遣ロジック
- `server/src/ai_combat.rs` — 攻撃遠征、ターゲットランキング
- `server/src/ai_economy.rs` — 施設建設、市場購入、魔獣生産
- `server/src/ai_actions.rs` — 統合エントリポイント（200行程度）

**5c. march.rs (893行) を分割**
- `server/src/model_actions/march_movement.rs` — 移動、経路計算
- `server/src/model_actions/march_resolution.rs` — 到着時処理、戦闘開始判定

---

## Task 6: バックエンド(Rust) — コード重複排除

**6a. Shared/PerPlayer 分岐の抽象化**
- `http_api.rs` L64-155 の register/login/exchange_token で繰り返される `GameStore::Shared`/`PerPlayer` 分岐をヘルパー関数 `with_game_store()` に統合
- `world_scheduler.rs`, `realtime.rs` の同パターンも順次統一

**6b. game_event.rs のボイラープレート削減**
- 20+の `push_xxx_event()` 関数の共通構造をマクロ `push_event!` で統一

---

## Task 7: バックエンド(Rust) — パフォーマンス最適化

**7a. realtime.rs の状態全体クローン削除**
- WebSocketハンドラー毎の `GameState` 全体ディープクローンを廃止
- `mutation_lock` 内で直接 `&mut GameState` を操作する方式に変更

**7b. world_scheduler.rs の並列化**
- PerPlayerモードの逐次tickを `tokio::spawn` + `join_all` で並列化

**7c. world_manager.rs のN+1クエリ修正**
- `saved_world_has_due_outbound_marches()` が各プレイヤーごとにフル状態をロードする問題を修正
- marchesテーブルのみを参照する軽量クエリに変更

---

## Task 8: 検証・テスト

- `cargo build` / `cargo clippy` でRust側のコンパイル・警告を確認
- `npm run build` でフロントエンドのビルドを確認
- 既存の動作確認（PvE/PvPモード）でリグレッションを検証

---

## 実行順序

Task 1 -> Task 2 -> Task 3 -> Task 4 -> Task 5 -> Task 6 -> Task 7 -> Task 8

Task 1-4 がフロントエンド、Task 5-7 がバックエンド、Task 8 が全体検証。各Task完了時点でビルド確認を行う。