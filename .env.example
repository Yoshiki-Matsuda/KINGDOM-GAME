# 開発時のみ。true にするとサーバー/DBに接続せず、仮のマスデータ（48x48）でマップを表示します。
# コピーして .env または .env.local にし、必要に応じて有効にしてください。
# VITE_USE_MOCK_STATE=true

# 地形イラストを無効にし、色のみで描画します。重いときは false にすると軽くなります。
# VITE_USE_TERRAIN_IMAGES=false
